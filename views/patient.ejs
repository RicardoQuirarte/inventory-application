<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <title><%= title %></title>
  </head>
  <body>
    <div class="container text-center" style="margin-top: 200px;">
      <h1><%= title %></h1>

      <table class="table table-striped table-hover">
        <thead class="table-dark">
          <tr>
            <th>Name</th>
            <th>Peso</th>
            <th>Estatura</th>
            <th>Age</th>
            <th>Price</th>
            <th>Category</th>
          </tr>
        </thead>
        <% if (user) {%> <% user.forEach(patient => { %>
          <tbody>
            <tr>
            <td><%= patient.name %></a></td>
            <td><%= patient.peso %></td>
            <td><%= patient.estatura %></td>
            <td><%= patient.age %></td>
            <td>$<%= patient.price %></td>
            <td><%= patient.category %></td>
          </tr>
          </tbody>
        <% }); %> <% } %>
      <table>

      <input name="id" id="id" value="<%= user.id %>" class="d-none" readonly />      

      <div class="btn-group">
        <a href="/" class="btn btn-outline-primary">Back to the main page</a>
        <a href="/update?id=<%= user[0].id %>" class="btn btn-outline-primary">Update patient</a>
        <form action="/delete?id=<%= user[0].id %>" method="POST">
          <button type="submit" class="btn btn-outline-danger">Delete patient</button>
        </form>
      </div>
    </div>
  </body>
</html>
